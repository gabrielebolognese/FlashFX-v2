# FlashFX environment variables. Copy to `.env` and fill in. NEVER commit `.env` (it's gitignored).
# All VITE_* values are exposed in the browser bundle — only put PUBLIC / publishable values here.

# ── Supabase (required) ──────────────────────────────────────────────────────
# Project Settings → API. The anon key is public/publishable (safe in the bundle).
# Do NOT put the service_role key here — it's server-only.
VITE_SUPABASE_URL=https://<your-project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon public key (starts with eyJ… or sb_publishable_…)>

# ── Social sign-in buttons (optional) ────────────────────────────────────────
# Which "Continue with …" buttons appear. Each provider is SHOWN by default; set a
# flag to "false" to HIDE that button until you've configured the provider. The
# provider CLIENT ID + SECRET are NOT set here — you paste them into the Supabase
# dashboard (Authentication → Providers). See docs/AUTH-SETUP.md for the full steps.
# Microsoft's Supabase provider id is "azure".
VITE_OAUTH_GOOGLE=true
VITE_OAUTH_MICROSOFT=true
VITE_OAUTH_APPLE=false

# ── Paddle billing (optional; billing turns ON only when token + price are set) ─
# Client-side token — public, like a Stripe publishable key. Sandbox tokens start
# with `test_`; live tokens start with `live_`. Developer Tools → Authentication.
VITE_PADDLE_CLIENT_TOKEN=
# The "FlashFX Pro" recurring price id (pri_…). SANDBOX and LIVE ids are DIFFERENT —
# use the live pri_ only once you've switched VITE_PADDLE_ENV to production.
VITE_PADDLE_PRICE_ID=
# 'sandbox' (default) while testing, 'production' when live.
VITE_PADDLE_ENV=sandbox
# Optional display price on the upgrade CTA.
VITE_PADDLE_PRICE_LABEL=$12/mo

# ── Local AI runs (node scripts only — NOT the browser) ──────────────────────
# scripts/director-run.mjs / coder-run.mjs read this from your shell env for real
# model runs. NEVER prefix it VITE_ (that would bundle the key into the browser).
# The in-app AI uses a browser BYOK key stored in localStorage, not this var.
# ANTHROPIC_API_KEY=sk-ant-...

# ── Edge-function secrets (NOT set here — set with `supabase secrets set …`) ────
# These live on the Supabase edge functions, never in the browser bundle:
#   PADDLE_WEBHOOK_SECRET   — Paddle notification signing secret (endpoint_secret_key).
#                             SANDBOX and LIVE destinations have DIFFERENT secrets.
#   SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY — injected automatically by Supabase.
#   GOOGLE_SERVICE_ACCOUNT_JSON — only for the (currently disabled) Drive proxy.
